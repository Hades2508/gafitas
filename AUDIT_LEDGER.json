{
 "audit_id": "GAFITAS_LOCAL_INDEPENDENCE",
 "opened": "2026-09-01",
 "closed": "2026-09-01",
 "canonical_root": "D:\\LOCAL-PROGRAMMER-ROOT",
 "canonical_package": "localprog",
 "canonical_rationale": "Only root with a single loop, a single non-drifting tool-schema source, borrowed containment with import-shadow detection, evidence sealing and a green deterministic suite. The duplicated runners in PROGRAMMER-BUILD-ROOT were removed in 59f8905; that repo keeps its real job as the source of programmer.guard and programmer.edit_channel. GENERALISTA-ROOT remains an organ donor and was not resurrected.",
 "suite": {
  "tests": 313,
  "state": "green"
 },
 "capability_map": {
  "ticket_intake": "WORKING  malformed ticket is HarnessInvalid, never a model failure",
  "workspace_worktree": "WORKING  git worktree; all 8 source repos verified unmodified after 50 runs",
  "containment": "WORKING  programmer.guard + shadow detection; 0 scope violations in 50 sealed runs; identical on both tiers",
  "discrimination_gate": "WORKING  a solved ticket costs 0 turns and 0 calls and is marked unscoreable",
  "model_provider": "WORKING  Ollama (native tools) and Codex/Luna (protocol J)",
  "turn_loop": "WORKING  one loop, five outcomes, four-class error taxonomy intact",
  "tools": "WORKING  10 tools, documented, single source of truth for schema+manual+validation",
  "read": "WORKING  bounded by characters as well as lines",
  "search": "WORKING  grep with context lines; list_dir with recursion",
  "tracing": "PARTIAL  list_symbols is per-file; no cross-file reference tracing. grep+context covers it in practice and no measured failure demanded more.",
  "edit": "WORKING  edit (unique match) and replace_lines (by line number)",
  "create_files": "WORKING  directory and glob write scopes",
  "execution": "WORKING  run: argv array, no shell, interpreter allowlist, git read-only, timeout, bounded output",
  "observation": "WORKING  exit codes and bounded output; every command recorded",
  "replanning": "WORKING  demonstrated: test fails -> edit -> fails -> edit -> green",
  "stall_detection": "WORKING  3 dead turns ends the run instead of burning 30",
  "context_management": "WORKING  per-payload ceiling, budgeted elision, bounded corrections",
  "behavioural_verification": "PARTIAL  5 deterministic refuse-only signals. No PRE/POST behavioural probes; the Behavioral Oracle is still a separate authority.",
  "rollback": "WORKING  the box is the rollback; preserved on failure (28 workspaces kept)",
  "evidence": "WORKING  sealed JSON + turn trace + readable patch, preserved even on abandoned runs",
  "telemetry": "PARTIAL  per-tier cost; LUNA reports no tokens and says so rather than printing zeros",
  "model_escalation": "WORKING  --tier AUTO, evidence-based, every outcome classified",
  "commit_handoff": "MISSING  a patch is produced; applying it is a human decision. Deliberate for now.",
  "explorer_integration": "MISSING  never demonstrated as needed"
 },
 "findings": [
  {
   "id": "F-01",
   "priority": "P0",
   "component": "step1/corpus",
   "status": "FIXED",
   "evidence": "All five STEP1 repos were snapshotted solved; acceptance passed PRE. STEP1_REPORT.md reads 5/5 tester_pass with every run BUDGET_EXHAUSTED.",
   "impact": "The only capability measurement ever taken was void.",
   "cause": "Acceptance was run only after the loop.",
   "fix": "verify.check_pre runs acceptance on the pristine workspace before the model is invoked; NON_DISCRIMINATING is unscoreable. Proved live: re-running two solved tickets costs 0 turns and 0 calls.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-02",
   "priority": "P0",
   "component": "tools.native_schema",
   "status": "FIXED",
   "evidence": "description == tool name; no parameter prose. qwen2.5-coder:7b scored 0/5 with 12/12 invalid calls.",
   "impact": "Models were asked to drive an undocumented API.",
   "cause": "Schema generated mechanically from SPECS.",
   "fix": "TOOL_DOC and PARAM_DOC, asserted equal to SPECS at import. Same source feeds the protocol-J text manual.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-03",
   "priority": "P0",
   "component": "tools",
   "status": "FIXED",
   "evidence": "No list_dir at all.",
   "impact": "Directories could not be enumerated.",
   "cause": "An earlier list_dir crashed on files and was deleted rather than fixed.",
   "fix": "Reinstated as a total function; a file yields its parent plus a note. Later gained recursion, implemented by GAFITAS itself (34c820a).",
   "risk": "low",
   "dependencies": [
    "F-02"
   ]
  },
  {
   "id": "F-04",
   "priority": "P0",
   "component": "execution",
   "status": "FIXED",
   "evidence": "Only run_tests could execute, only against declared node ids.",
   "impact": "No RUN -> OBSERVE -> DEBUG half of the loop.",
   "cause": "Contract scope.",
   "fix": "run: argv array, shell=False, interpreter allowlist, git restricted to read-only, timeout, bounded output.",
   "risk": "medium, mitigated structurally",
   "dependencies": []
  },
  {
   "id": "F-05",
   "priority": "P0",
   "component": "write scope",
   "status": "FIXED",
   "evidence": "Membership was string equality against a list.",
   "impact": "Only files the author named in advance could be created.",
   "cause": "Scope modelled as a path list.",
   "fix": "scope.py: file, dir/ and glob patterns. Containment unchanged and still decided by programmer.guard.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-06",
   "priority": "P1",
   "component": "budget",
   "status": "FIXED",
   "evidence": "MAX_TURNS 12; every STEP1 run hit it.",
   "impact": "The full loop did not fit.",
   "cause": "Screen budget reused for real work.",
   "fix": "WORK_MAX_TURNS 40; the frozen screen keeps 12.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-07",
   "priority": "P1",
   "component": "elision",
   "status": "FIXED",
   "evidence": "Results older than 3 turns and >500 chars replaced.",
   "impact": "Working memory destroyed exactly when debugging needs it.",
   "cause": "Tuned for an 8192 context.",
   "fix": "Per-instance parameters; working defaults 8 turns / 2500 chars.",
   "risk": "low",
   "dependencies": [
    "F-06"
   ]
  },
  {
   "id": "F-08",
   "priority": "P1",
   "component": "telemetry",
   "status": "FIXED",
   "evidence": "prompt_eval_count/eval_count discarded.",
   "impact": "No cost visibility.",
   "cause": "Never built.",
   "fix": "Per-call usage, accumulated, split by model class.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-09",
   "priority": "P1",
   "component": "verification",
   "status": "FIXED",
   "evidence": "Verdict was the pytest exit code alone.",
   "impact": "TESTS GREEN = SUCCESS with no check that the suite discriminates or that unrequested behaviour survived.",
   "cause": "Never built.",
   "fix": "verify.py: 5 refuse-only signals. combine() returns the worst verdict and there is no path from non-PASS to PASS.",
   "risk": "medium (false blocks) - two were found and fixed, F-24 and F-36",
   "dependencies": [
    "F-01"
   ]
  },
  {
   "id": "F-10",
   "priority": "P1",
   "component": "tools.finish",
   "status": "FIXED",
   "evidence": "finish refused unless something had been edited.",
   "impact": "No way to report being blocked.",
   "cause": "Designed for the screen's single-edit task.",
   "fix": "finish(status=DONE|NO_CHANGE|BLOCKED).",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-11",
   "priority": "P1",
   "component": "entrypoint",
   "status": "FIXED",
   "evidence": "CLI exposed only gate instruments.",
   "impact": "No way to hand it a ticket.",
   "cause": "Built as a gate.",
   "fix": "work.py and `localprog work`.",
   "risk": "low",
   "dependencies": [
    "F-01",
    "F-03",
    "F-04",
    "F-05"
   ]
  },
  {
   "id": "F-12",
   "priority": "P2",
   "component": "PROGRAMMER-BUILD-ROOT",
   "status": "FIXED",
   "evidence": "screen_runner.py / step1_runner.py duplicated the loop and had already diverged.",
   "impact": "The shadowing class that already produced one invalid-results incident.",
   "cause": "Built before the canonical root existed.",
   "fix": "Removed in 59f8905 with docs/CANONICAL_PROGRAMMER.md explaining why. Git history is the archive.",
   "risk": "low",
   "dependencies": [
    "F-11"
   ]
  },
  {
   "id": "F-13",
   "priority": "P1",
   "component": "escalation",
   "status": "FIXED",
   "evidence": "No routing anywhere; the Codex adapter was orphaned in GATE-A-WS.",
   "impact": "No LOCAL -> LUNA -> CLAUDE ladder.",
   "cause": "Never built.",
   "fix": "route.py + --tier AUTO. Three groups: escalate, keep, refuse. Refuse is the important one - it stops corpus defects and crashed servers becoming paid API calls.",
   "risk": "low",
   "dependencies": [
    "F-11",
    "F-08"
   ]
  },
  {
   "id": "F-14",
   "priority": "P3",
   "component": "explorer",
   "status": "OPEN",
   "evidence": "Explorer has rank_repo/context_pack; localprog never calls it.",
   "impact": "Optional ranking unavailable.",
   "cause": "Deliberate - a mandatory pre-filter was the expensive past mistake.",
   "fix": "Expose only if a measured exploration failure demands it. None has.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-15",
   "priority": "P0",
   "component": "loop",
   "status": "FIXED",
   "evidence": "ga04: turns 13-40 all produced no tool call. 28 dead turns, 61k output tokens, 13 minutes.",
   "impact": "70% of a budget spent re-observing the same fact.",
   "cause": "'The agent has stopped acting' was not a stopping condition.",
   "fix": "STALLED outcome after 3 consecutive dead turns. Scoreable; a single bad reply is not a stall and recovery resets it.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-16",
   "priority": "P0",
   "component": "transcript",
   "status": "FIXED",
   "evidence": "A dead turn appended the model's entire reply, up to num_predict.",
   "impact": "Failing made the prompt bigger, which made failing likelier. A runaway.",
   "cause": "add_correction kept the whole message.",
   "fix": "600-character excerpt; the feedback itself is never truncated.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-17",
   "priority": "P1",
   "component": "transcript",
   "status": "FIXED",
   "evidence": "A write_file carrying 5 KB of new module source was echoed on every later turn.",
   "impact": "Context exhausted by payloads nobody needed twice.",
   "cause": "INVARIANTS I11 exempted tool calls from elision.",
   "fix": "The call is always kept; an oversized argument VALUE is replaced by a description.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-18",
   "priority": "P1",
   "component": "transcript",
   "status": "FIXED",
   "evidence": "Elision driven only by turn age.",
   "impact": "A transcript could exceed num_ctx while every turn looked reasonable.",
   "cause": "Age is a poor proxy for size.",
   "fix": "Character budget from num_ctx, oldest-first. Soft target, hard floor: the last keep_turns are never elided, because that would cure the context wall by causing amnesia.",
   "risk": "low",
   "dependencies": [
    "F-06"
   ]
  },
  {
   "id": "F-19",
   "priority": "P2",
   "component": "evidence",
   "status": "FIXED",
   "evidence": "Per-turn prompt size was not recorded.",
   "impact": "A context wall had to be inferred from a summed token count afterwards.",
   "cause": "Never built.",
   "fix": "prompt_tokens on every event.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-20",
   "priority": "P1",
   "component": "screen",
   "status": "FIXED",
   "evidence": "The frozen screen would have declared 10 tools while its contract's prompt names 7.",
   "impact": "A frozen instrument silently changing what it measures; results incomparable with sealed runs.",
   "cause": "Schema generated from SPECS, which grew.",
   "fix": "native_schema(only=...) restricts what is DECLARED without restricting dispatch.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-21",
   "priority": "P1",
   "component": "step1",
   "status": "FIXED",
   "evidence": "step1 had the same missing PRE check that voided its own corpus.",
   "impact": "It could mint another invalid number.",
   "cause": "Same as F-01.",
   "fix": "Same gate; refuses NON_DISCRIMINATING missions.",
   "risk": "low",
   "dependencies": [
    "F-01"
   ]
  },
  {
   "id": "F-22",
   "priority": "P1",
   "component": "loop feedback",
   "status": "FIXED",
   "evidence": "ga04 spent its last 16 turns alternating run/run_tests with no edit between.",
   "impact": "The agent could not make the decision a deadline forces.",
   "cause": "The turn budget was stated once, at the start, and never again.",
   "fix": "A countdown once 8 turns remain, and an explicit last-turn notice.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-23",
   "priority": "P1",
   "component": "loop feedback",
   "status": "FIXED",
   "evidence": "Byte-identical failure output read as new information, sixteen times.",
   "impact": "The most informative event in debugging was withheld.",
   "cause": "The harness had both results and compared neither.",
   "fix": "An identical result twice running says the work in between changed nothing measured.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-24",
   "priority": "P0",
   "component": "conscience boundary",
   "status": "FIXED",
   "evidence": "ga06 produced a module taking its suite from a collection error to 22 passed, broke none of 11 passing tests, stayed in scope - and was refused because the agent had said BLOCKED.",
   "impact": "I9 applied backwards: the model's confidence overruled six pieces of agreeing evidence.",
   "cause": "The self-report was folded in with the deterministic signals.",
   "fix": "The conscience holds evidence about the CODE; the agent's claim is metadata. New outcome PASS_UNCONFIRMED - real work with a flag, not a failure. It cannot launder broken work.",
   "risk": "low",
   "dependencies": [
    "F-09"
   ]
  },
  {
   "id": "F-25",
   "priority": "P0",
   "component": "tools.read_file / transcript",
   "status": "FIXED",
   "evidence": "Found by dogfooding. read_file on a 47,945-character file returned 400 exceed_context_size_error on turn 2.",
   "impact": "One correct, obvious action ended the run.",
   "cause": "read_file capped at 2000 LINES; the elision budget's hard floor leaves one oversized recent payload unbounded.",
   "fix": "Caps at both ends, plus a named CONTEXT_OVERFLOW ProviderError kind so the defect is visible and stays unscoreable.",
   "risk": "low",
   "dependencies": [
    "F-18"
   ]
  },
  {
   "id": "F-26",
   "priority": "P0",
   "component": "editing",
   "status": "FIXED",
   "evidence": "Dogfood attempt 2 failed six times running with ERROR_NO_MATCH, re-reading between every attempt.",
   "impact": "edit requires bytes reproduced exactly; on a 48k file the read that showed them had been elided. The agent was asked to quote from memory a document it was not allowed to keep.",
   "cause": "One editing primitive, and it needed perfect recall.",
   "fix": "replace_lines(path, start, end, content). Line numbers come free with read_file and list_symbols and survive elision. edit stays: matching text is self-verifying.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-27",
   "priority": "P1",
   "component": "tools.edit",
   "status": "FIXED",
   "evidence": "ERROR_NO_MATCH said the text was not found and to read the file - the loop the agent was already stuck in.",
   "impact": "A failed edit left the agent informed but unable to act.",
   "cause": "The message described the problem, not the state.",
   "fix": "Shows the nearest region as it actually stands (difflib) and names replace_lines. Omitted when nothing is close.",
   "risk": "low",
   "dependencies": [
    "F-26"
   ]
  },
  {
   "id": "F-28",
   "priority": "P1",
   "component": "provider",
   "status": "FIXED",
   "evidence": "The dogfood run spent its whole life pegged at 16,383 tokens.",
   "impact": "Constant re-reading.",
   "cause": "16384 inherited from the screen's 7B-on-12GB constraint.",
   "fix": "WORK_NUM_CTX 32768. For the 4B doing this work, ~7.2 GB total inside 12 GB.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-29",
   "priority": "P0",
   "component": "tools.finish",
   "status": "FIXED",
   "evidence": "Dogfood attempt 3 edited correctly and declared DONE on turn 12 of 40 without ever calling run_tests.",
   "impact": "A claim of completion from an agent that never looked.",
   "cause": "The prompt said to run the tests; nothing enforced it.",
   "fix": "DONE requires a green run_tests. Tickets with no declared tests can still finish, so it cannot force a BLOCKED.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-30",
   "priority": "P1",
   "component": "loop",
   "status": "FIXED",
   "evidence": "qwen3.5:9b lost 30 turns of work to one malformed tool call the server rejected with a 500.",
   "impact": "Sampling noise in a single generation ended an entire run.",
   "cause": "No retry.",
   "fix": "Bounded retry (2 extra), counted. CONTEXT_OVERFLOW excluded: it is deterministic and retrying only delays a defect that is ours.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-31",
   "priority": "P0",
   "component": "work",
   "status": "FIXED",
   "evidence": "A run that made five edits sealed a record saying it changed nothing.",
   "impact": "A failure destroying its own evidence on the way out - what experiments.md exists to forbid.",
   "cause": "The abort path returned before comparing the filesystem.",
   "fix": "Abandoned runs record what changed and write the patch.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-32",
   "priority": "P2",
   "component": "tools",
   "status": "FIXED",
   "evidence": "24 of 40 turns spent on read_file.",
   "impact": "Crossing a 48k file cost eight calls, and elision undid them.",
   "cause": "MAX_TOOL_PAYLOAD_CHARS sized against a 16k window that became 32k.",
   "fix": "12000 -> 20000.",
   "risk": "low",
   "dependencies": [
    "F-28"
   ]
  },
  {
   "id": "F-33",
   "priority": "P1",
   "component": "tools.grep",
   "status": "FIXED",
   "evidence": "Same run: grep said where the code was and nothing about what it said.",
   "impact": "Every lookup cost two calls, the second returning mostly irrelevant lines.",
   "cause": "grep returned only matching lines.",
   "fix": "context=N, numbered so it can feed replace_lines directly. Bounded by the payload ceiling.",
   "risk": "low",
   "dependencies": [
    "F-32"
   ]
  },
  {
   "id": "F-34",
   "priority": "P1",
   "component": "loop feedback",
   "status": "FIXED",
   "evidence": "Same model, same ticket, twice: once 5/6 tests green, once 25 reads, 10 greps, 4 runs and zero edits in 40 turns.",
   "impact": "An agent exploring forever, unremarked.",
   "cause": "No signal for 'you have changed nothing at all'.",
   "fix": "A nudge after 12 turns with an empty diff.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-35",
   "priority": "P0",
   "component": "provider.CodexProvider",
   "status": "FIXED",
   "evidence": "codex exited 1: input is not valid UTF-8 at offset 34935, inside a Spanish docstring the agent had just read.",
   "impact": "One accented character anywhere in the repository killed the run.",
   "cause": "subprocess text=True encodes stdin with the locale codec (cp1252 here).",
   "fix": "encoding='utf-8', errors='replace'.",
   "risk": "low",
   "dependencies": [
    "F-13"
   ]
  },
  {
   "id": "F-36",
   "priority": "P0",
   "component": "conscience",
   "status": "FIXED",
   "evidence": "The self-dogfood run solved its ticket -- 6/6 acceptance, 241 preserved tests, scope respected -- and was blocked because grep had gained an optional argument. Which was the task.",
   "impact": "The conscience obstructing exactly the work it protects.",
   "cause": "Parameter names compared as a tuple, so any difference read as a break.",
   "fix": "Compatible exactly when the old list is a prefix of the new and every addition has a default. Removals, renames, reorderings and new required parameters still block.",
   "risk": "low",
   "dependencies": [
    "F-09"
   ]
  },
  {
   "id": "F-37",
   "priority": "P1",
   "component": "route accounting",
   "status": "FIXED",
   "evidence": "The sweep showed LUNA with 0 tok_in and 0 tok_out -- the only tier that costs money.",
   "impact": "Zero reads as free. The exact blind spot this project was told not to repeat.",
   "cause": "The Codex CLI reports no usage and zeros were printed anyway.",
   "fix": "tokens_reported per tier; the report says tokens are not reported rather than printing a number that is not one.",
   "risk": "low",
   "dependencies": [
    "F-13"
   ]
  },
  {
   "id": "F-38",
   "priority": "P0",
   "component": "tools.run_tests / finish",
   "status": "FIXED",
   "evidence": "tests01 failed in both sweeps and was escalated to a paid tier. LOCAL had done the work -- 3 failing acceptance tests turned green -- and was failed by the conscience for leaving one of three legacy tests red.",
   "impact": "The agent was judged on a criterion the harness refused to show it: run_tests ran the ACCEPTANCE suite only, so it saw green and stopped, while the collateral comparison happened after the loop.",
   "cause": "The feedback channel was narrower than the verdict.",
   "fix": "When the acceptance goes green the collateral check runs and the answer is appended to that same tool result. finish(DONE) refuses while a known regression stands.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-39",
   "priority": "P0",
   "component": "tools syntax errors",
   "status": "FIXED",
   "evidence": "ga06, both sweeps: 20 and 25 consecutive write_file calls, every one ERROR_SYNTAX_AFTER_EDIT.",
   "impact": "The feedback said 'line 23: unexpected indent' -- line 23 of the model's own past output, already elided, which it had no way to look at.",
   "cause": "The error named the fault without showing the state.",
   "fix": "Syntax errors quote the offending line with context and a caret. ga06 went from 25 failures to a pass in 5 turns.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-41",
   "priority": "P1",
   "component": "provider preflight",
   "status": "FIXED",
   "evidence": "An 8-ticket LOCAL sweep produced 8 PROVIDER_ERRORs and reported 0/8 PASS because Ollama had stopped.",
   "impact": "A report that reads like eight failures, and an afternoon of GPU time that never happened.",
   "cause": "No liveness check before a batch.",
   "fix": "One request before the batch: no server, or that model is not installed, and here is what is.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-42",
   "priority": "P1",
   "component": "finish refusals",
   "status": "FIXED",
   "evidence": "ga04 reached 14/16 then spent ten turns on finish(DONE) -> refused -> blind edit -> NO_MATCH -> finish again.",
   "impact": "The refusal said to run the tests and read the output. It had; what it could not do was remember which one failed.",
   "cause": "The refusal carried no state.",
   "fix": "Refusals quote the failing test names and the last error.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-43",
   "priority": "P1",
   "component": "loop feedback",
   "status": "FIXED",
   "evidence": "Ten identical refusals in ga04, acted on none.",
   "impact": "Feedback already arriving was not reaching the agent's decisions.",
   "cause": "No signal for a repeated identical ERROR, only for a repeated identical RESULT.",
   "fix": "Named bluntly at the third occurrence -- by then the gentler version has been tried twice.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-45",
   "priority": "P1",
   "component": "tools.finish",
   "status": "FIXED",
   "evidence": "ga07 gave up on turn 10 of 40 after one failed import fix.",
   "impact": "'I tried once and it did not work' is not 'I cannot do this', and the harness could not tell them apart.",
   "cause": "BLOCKED was accepted unconditionally.",
   "fix": "The first BLOCKED with real budget left and never-green tests is answered with the last failure and one question. A second is always accepted.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-46",
   "priority": "P0",
   "component": "TEST_OUTPUT_TAIL",
   "status": "FIXED",
   "evidence": "Every run_tests result in the ga04/ga07/ga08 failures came back at 3131-3171 characters: clipped, every time.",
   "impact": "The most important feedback channel in the loop was also the most aggressively truncated thing in the system.",
   "cause": "3000 characters, sized for the original 8192-token context and never revisited.",
   "fix": "Raised to 9000, pytest run with --tb=short -rf, and failing test names returned structurally so they survive clipping.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-48",
   "priority": "P0",
   "component": "route",
   "status": "FIXED",
   "evidence": "24-run measurement: seven of eight tickets passed at least once in three, and only ga04 never did. As one number that is 62.5% and looks like a ceiling.",
   "impact": "Escalating on the first local miss paid for work the local model does two times in three.",
   "cause": "One attempt per tier.",
   "fix": "Per-tier retries: LOCAL_FAST xN, LOCAL_STRONG xM, LUNA only with --allow-luna. Retrying a free tier is persistence, not escalation, and is not counted as such.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-49",
   "priority": "P1",
   "component": "evidence",
   "status": "FIXED",
   "evidence": "Three local attempts sealed to one filename; the first two vanished.",
   "impact": "The attempts that FAILED are the ones worth reading, and a failure destroying its own record is what experiments.md forbids.",
   "cause": "The seal stem did not include the attempt.",
   "fix": "Retried attempts seal separately.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-50",
   "priority": "P0",
   "component": "tools.read_symbol",
   "status": "FIXED",
   "evidence": "DOGFOOD-03: the 9B spent 30 of 40 turns on 19 reads and 11 greps and never ran the tests once.",
   "impact": "Reading one named function out of a 1200-line file cost three calls plus arithmetic, and neither model reliably composed it -- the 9B never called list_symbols at all.",
   "cause": "No primitive for the commonest navigation a programmer does.",
   "fix": "read_symbol(path, name): AST-resolved, real line numbers that feed replace_lines, decorators included, and it lists what IS there when the name is wrong.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-51",
   "priority": "P0",
   "component": "route",
   "status": "FIXED",
   "evidence": "A transient 500 on the third local attempt ended a whole run; the free strong tier below it was never reached.",
   "impact": "'Do not pay for infrastructure failure' had been implemented as 'give up entirely'.",
   "cause": "PROVIDER_ERROR and NON_DISCRIMINATING shared one TERMINAL set.",
   "fix": "Split into ABANDON (a non-task stays a non-task at any tier) and INFRASTRUCTURE (retry while it is free, never climb into a paid tier on it).",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-52",
   "priority": "P1",
   "component": "num_predict",
   "status": "FIXED",
   "evidence": "500 invalid tool call arguments for 'edit': unexpected end of JSON input.",
   "impact": "Looked like a broken model; was the model being cut off mid-argument.",
   "cause": "num_predict 2048, too small for an edit carrying a hundred lines of new code.",
   "fix": "Raised to 4096.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-53",
   "priority": "P0",
   "component": "transcript budget",
   "status": "FIXED",
   "evidence": "The 9B's prompt never passed 17k of an available 32k while it spent 28 of 40 turns re-reading files the budget had just discarded.",
   "impact": "The agent was being throttled below the context it actually had, and then re-reading what the throttle discarded.",
   "cause": "CONTEXT_FRACTION 0.55, chosen for a 16k window, reserving ~15k tokens for a schema costing ~2.8k.",
   "fix": "The reservation is now measured: real schema, real system prompt, requested num_predict, declared margin. 23.7k tokens at 32k -- and correctly SMALLER at 16k, where the old fraction was over-optimistic and caused the earlier overflow 400s.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-54",
   "priority": "P0",
   "component": "list_symbols / read_symbol",
   "status": "FIXED",
   "evidence": "Three DOGFOOD-03 attempts died leaving tools.py unimportable on 'every parameter in SPECS needs an entry in PARAM_DOC' -- a clear 786-character message delivered four times and never acted on.",
   "impact": "The agent had no way to FIND PARAM_DOC: both navigation tools walked only FunctionDef, AsyncFunctionDef and ClassDef, so every module-level table was invisible.",
   "cause": "Navigation blind to module-level data, which is what tickets most often ask to change.",
   "fix": "Both tools see module- and class-level assignments, annotated ones included. Tuple unpacking is skipped: 'A, B = f()' has no source range meaning 'A'.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-55",
   "priority": "P2",
   "component": "local capability boundary",
   "status": "OPEN",
   "evidence": "DOGFOOD-03: thirteen free attempts across the 4B and the 9B, every one landing between 4 and 5 of 11.",
   "impact": "A subtle symbol-filtering rule across five coordinated declaration sites in a 1450-line module whose module-level asserts make a partial edit un-importable rather than merely wrong.",
   "cause": "Above the local tier as it stands.",
   "fix": "Recorded as UNSUPPORTED and xfail-marked in the suite rather than quietly routed to a paid API. The feature is still wanted and the tests are still right; the boundary is written down.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-56",
   "priority": "P2",
   "component": "thermals and VRAM",
   "status": "FIXED",
   "evidence": "A 9B sat on 6.6 GB of VRAM with the fans at 83% for five minutes after every batch.",
   "impact": "This machine is also the operator's desktop.",
   "cause": "Ollama's default keep_alive of five minutes.",
   "fix": "keep_alive is bounded and the batch releases the model when it finishes. Idle after a run: 47 C, 19 W, fan off, 1.2 GB.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-58",
   "priority": "P1",
   "component": "evidence",
   "status": "FIXED",
   "evidence": "The first retrieval audit could not name a single query the agent had run. 100 sealed RepoQA runs, 470 grep calls, and not one pattern recoverable.",
   "impact": "evidence.py's own docstring said 'every tool call, its arguments, its result classification and the diff are written before anything is cleaned up'. The arguments were never written. A transcript without the queries cannot answer the one question a retrieval audit asks, so the defect only surfaced when 46 of 100 runs failed at search and there was no way to see what they searched for.",
   "cause": "The event record carried tool name, ok, result size and repeat count, never the call.",
   "fix": "loop.py seals a truncated copy of the arguments on every tool event, ok or not. 300 characters per value: enough to read back a query, not enough to store a file being written.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-59",
   "priority": "P0",
   "component": "retrieval",
   "status": "FIXED",
   "evidence": "RepoQA python: the same model scores 85% when handed the function inside a prompt and 19% when it has to find it in the repository. Independent classification of the 100 runs: 46 NO_ANSWER, 33 WRONG_SYMBOL, 19 CORRECT, 2 formatting. 79 of the 81 failures are retrieval. 60% of all grep calls returned nothing, and the patterns show why: the model types semantic queries -- 'scaling factor buffer', 'merge|combine|adjacent|strings' -- into a line-literal regex tool.",
   "impact": "GAFITAS had no way to ask 'which of these four thousand functions is the one being described'. list_dir, grep and read_symbol all require you to already suspect where to look. The model was reasoning correctly about WHAT to search for and had no tool that could accept the search.",
   "cause": "A missing organ, not a weak model.",
   "fix": "localprog/retrieval.py: a BM25 index over definition-delimited regions, language-agnostic by design (Python's ast would have made the fix a Python trick), textbook constants, stdlib only, ~0.3s to build a 600-file repository. Exposed as the search_code tool. Offline on the 100 dev needles it puts the right function in the top 15 of ~4500 candidates for 82 of them, median rank 3.",
   "risk": "medium",
   "dependencies": [
    "F-58"
   ]
  },
  {
   "id": "F-60",
   "priority": "P1",
   "component": "navigation memory / tool feedback",
   "status": "FIXED",
   "evidence": "One run spent 24 of its 30 turns cycling the same eight grep patterns three times over, every one of them empty, and was told '0 coincidencias' 24 times. The loop held the whole history and said none of it.",
   "impact": "The transcript window is finite, so an agent twelve turns into a search cannot see the query it ran on turn two. The harness can. Staying quiet about it is how a run spends its budget re-proving that a term is not in the repository.",
   "cause": "Repeat detection only fired on byte-identical CONSECUTIVE results, and a failed search left no trace at all.",
   "fix": "The loop names an exact repeat with the turn it first happened, keeps the list of searches that came back empty and shows it from the third, and seals both. grep now reports what the system already knew and was withholding: whether the glob matched no file at all and which extensions do exist, whether ignore_case would match, which words of a phrase appear anywhere, and -- when a result is cut at 50 hits -- how many matches there really are and in which files.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-61",
   "priority": "P0",
   "component": "finish contract",
   "status": "FIXED",
   "evidence": "Seven RepoQA runs ended with the agent's own summary saying the answer file had been written -- 'su codigo completo se ha escrito en REPOQA_ANSWER.txt' -- having never called write_file once. finish refused them for having changed nothing, they repeated the call, and the loop recorded a deliberate ending.",
   "impact": "ANY refused finish could be turned into an accepted one by making the same call again. A rule that converts 'you have not done the work' into 'confirmed, there was no work' on the second attempt launders failure into completion, and it did.",
   "cause": "The double-finish escape predates finish(status='NO_CHANGE') (F-10) and was never removed when the explicit status made it redundant.",
   "fix": "The escape is gone: repeating a refused call never changes the answer. NO_CHANGE with an empty diff now gets the same single question BLOCKED already gets (F-45), asked with facts -- nothing was created or modified, and this is what the mission authorised you to create. A second NO_CHANGE is accepted immediately.",
   "risk": "medium",
   "dependencies": []
  },
  {
   "id": "F-62",
   "priority": "P2",
   "component": "Explorer integration",
   "status": "MEASURED_AND_DECLINED",
   "evidence": "Same 100 dev needles, same offline protocol: Explorer's rank_repo puts the needle's FILE in its top 10 for 55 of 100 and the needle's SYMBOL for 31 of 100 (and ranks the symbol at all for only 51). The BM25 region index reaches 78 and 82.",
   "impact": "Explorer is a real authority with a real API and it was the obvious thing to reach for. It ranks keyword matches against file paths, symbol NAMES and imports, which is the wrong signal for an objective written as prose.",
   "cause": "Different problem. Not a defect in Explorer.",
   "fix": "NOT INTEGRATED. It was measured against the observed cluster before being adopted, and it does not attack it. Explorer stays available and unused here; if a future cluster turns out to be dependency or reference tracing, its graph is the right tool for that and this measurement says nothing against it.",
   "risk": "low",
   "dependencies": []
  },
  {
   "id": "F-72",
   "priority": "P0",
   "component": "protocol / provider boundary",
   "status": "FIXED",
   "evidence": "Payload probe, 3 sizes x 2 channels x 3 engines. qwen2.5-coder:3b reproduces 400/1200/3000 characters of source as prose with line_recall 1.00 and got 0 of 3 through write_file. What it emitted was a correct call -- right name, right argument, right bytes -- whose JSON was broken by the docstring quotes inside its own payload. Ollama rejected it, message.tool_calls came back empty, the harness reported ERROR_NO_TOOL_CALL and discarded all of it. After repair.py: 3 of 3, recall 1.00 at every size.",
   "impact": "The engine was recorded as supports_native_tools=false and scored 0% through GAFITAS against 52% in one prompt. Fifty runs were charged to the model for a harness that threw away work it had already done.",
   "cause": "Both parsers treated 'the provider found no call' as 'the engine emitted no call'. Those are different facts.",
   "fix": "localprog/repair.py. Three bounded deterministic tiers, reached ONLY from the raise site so a well-formed engine can never take the path; a recovered name that is not a real tool is refused; every recovery announced on ParsedCall.repaired. Red-teamed by Codex: oversized input is refused rather than half-recovered (it was silently truncating a 400k payload), and an object naming the same key twice is refused."
  },
  {
   "id": "F-73",
   "priority": "P0",
   "component": "engine capabilities",
   "status": "FIXED",
   "evidence": "granite4.1:3b native channel: 200 ok, 400 ok, 800 ok, 1600 LOST -- above the cliff Ollama's parser for its template returns content EMPTY and tool_calls EMPTY, ~300 generated tokens gone, nothing left to repair. Text protocol, same engine, same sizes: every rung carried.",
   "impact": "supports_native_tools=true was TRUE and worthless. The whole granite campaign ran on the protocol that could not carry a payload.",
   "cause": "'Can it call a tool' was the wrong question. The question is how much it can carry through one, and that has a number.",
   "fix": "native_payload_limit and text_payload_limit on EngineCapabilities, measured by persistence.measure_payload_limit, compared against PAYLOAD_FLOOR to choose the protocol. An unmeasured limit changes nothing, so every engine certified earlier keeps its protocol and its numbers stay comparable."
  },
  {
   "id": "F-74",
   "priority": "P1",
   "component": "tool surface",
   "status": "FIXED",
   "evidence": "granite matched, first 23 runs after F-72: first tool called was edit 18 times, read_file 3, grep 2. ERROR_NOT_IN_WRITE_SCOPE 18, ERROR_NOTHING_CHANGED 33, only 5 of 23 runs looked at anything, median 3 turns. After legal_tools: first tool read_file 7, edit 4, search_code 1; 10 of 12 looked; 12 of 12 CANDIDATE.",
   "impact": "The mission's write scope was empty and its only writable thing did not exist yet, so edit could not succeed for ANY argument. We declared it, the engine took the tool matching the verb in the objective, and quit.",
   "cause": "The declared surface was constant. It is a function of mission state.",
   "fix": "tools.legal_tools(write_scope, allowed_new_files), used for the schema and the text manual alike so the two tiers cannot disagree. Looking and finishing are never withheld. An ordinary ticket keeps all twelve tools. ERROR_NOT_IN_WRITE_SCOPE now leads with the work, as finish() did in F-71."
  },
  {
   "id": "F-75",
   "priority": "P1",
   "component": "write scope",
   "status": "FIXED",
   "evidence": "ERROR_FILE_EXISTS 7 times in 12 granite runs. write_file refused with 'ya existe, usa edit'; edit refused with 'fuera de write_scope' because the scope was empty. No third door.",
   "impact": "On a create-only mission the agent's own output file was unreachable after its first draft. The reference engine almost never hits it because it usually writes once and correctly -- which is how a defect survives being measured against a single engine.",
   "cause": "allows(rel, creating=False) was false for a path the mission had just authorised the agent to create.",
   "fix": "ToolContext.created_files: a file THIS RUN created may be rewritten. Keyed on what was actually created, not on allowed_new_files, so a mission naming an existing source file grants nothing. scope.py untouched."
  },
  {
   "id": "F-76",
   "priority": "P2",
   "component": "reporting",
   "status": "CORRECTED",
   "evidence": "matched_python_pre_channel_fixes 74/66, _r2 72/62, _r3 74/64, matched_python 74/76. Five dogfood08 acceptance tests have been failing at HEAD since f97cde2.",
   "impact": "I reported '72 vs 66' as the standing 4B result when the most recent run was 74/76, and reported '521 passed' when five tests were red.",
   "cause": "Quoting an earlier run and an earlier tally instead of re-reading them.",
   "fix": "The 4B's matched result varies 62-76 against a stable 72-74 baseline, so no single run demonstrates amplification and it is reported as PRESERVED with the spread stated. dogfood08's suite is xfail with its FAIL recorded."
  },
  {
   "id": "F-77",
   "priority": "P1",
   "component": "efficiency / prompt",
   "status": "FIXED_NOT_YET_MEASURED",
   "evidence": "Fifty matched runs of the reference engine, 426 tool calls: list_dir 140 -- turn 1 in 49 of 50 runs, turn 2 in 38, turn 3 in 24 -- on missions that NAME the file, with the first write at median turn 7.",
   "impact": "A quarter of every run spent establishing what the harness already knew. list_dir was the most-used tool in the whole corpus.",
   "cause": "The working instructions opened with 'ORIENTATE. Si no conoces el repositorio, empieza por list_dir' and the agent did as it was told.",
   "fix": "localprog/orient.py: a bounded deterministic repo map in the objective. Takes no objective and no query, so it cannot become a retrieval channel; says when it truncated, because an agent that thinks it has seen everything stops looking; skips what the index skips. The turn and token counts are the claim and the next matched run is the test."
  },
  {
   "id": "F-78",
   "priority": "P1",
   "component": "tools",
   "status": "ADDED_NOT_YET_MEASURED",
   "evidence": "granite4.1:3b with legal_tools: 48.89% MODEL_ONLY vs 4.44% GAFITAS, 45/45 CANDIDATE -- it looks, it writes, it no longer quits, and the content is wrong. 14 of its 35 written answers are longer than its measured 1600-character text payload limit. read_symbol's docstring records the same cost for the reference engine: 72% byte-exact as plain text against 30% read-through-tools-and-written-back.",
   "impact": "Every write in this harness makes the engine retype text the harness already has on disk. For a small engine that is disqualifying rather than wasteful: there are answers it knows and cannot emit.",
   "cause": "No tool moved bytes. All of them moved them THROUGH the model.",
   "fix": "copy_code(src, into, name=|start=/end=). Returns source, range, byte count and sha256 instead of the body, so a reviewer can check the bytes came off disk and the transcript does not get back the weight the tool exists to keep out. The matched task IS copy-a-function, so any matched gain is upper-bounded by the task's shape; dogfood08 -- extract-to-module, failed 3/3, byte-identity in its frozen acceptance -- is the general test."
  },
  {
   "id": "F-79",
   "priority": "P1",
   "component": "retrieval scope",
   "status": "FIXED",
   "evidence": "Reproduced on the real corpus: search_code('returns a string of newlines and optionally a form feed', path='src/black/nodes.py') ranked src/black/comments.py first. ERROR_NO_MATCH was the most common error in the whole 50-run reference corpus, 33 of them. With the file honoured, make_simple_prefix -- the function that run spent 15 turns failing to find, finishing with nothing written -- ranks first.",
   "impact": "On a mission whose objective NAMES the file, the agent scoped its search to that file and was answered from a different one, silently.",
   "cause": "path was resolved as a DIRECTORY prefix, so a file became the prefix 'src/black/nodes.py/', nothing was indexed under it, and F-69's scope note correctly reported 'nothing under that prefix' and widened to everything. Every step behaved as written; their sum discarded the constraint.",
   "fix": "A file path filters to that file. F-69 keeps the case it was built for -- a prefix that names nothing. A file that exists but carves no regions says so rather than answering from elsewhere."
  },
  {
   "id": "F-80",
   "priority": "P2",
   "component": "tools",
   "status": "FIXED",
   "evidence": "granite4.1:3b, first copy_code run: t1 copy_code(src=..., into=...) with the selector missing -> 'para ver que hay: list_symbols o read_file'; t2 read_file; t3 finish refused; t4 wrote the DESCRIPTION into the answer file. It never came back to copy_code. Adoption was nonetheless 7 of 12 runs, and copy_code was the FIRST tool in 7 -- against copy_region's historical 0 uses.",
   "impact": "Three turns and the thread, on a call that was already correct except for one argument.",
   "cause": "The refusal named two other tools instead of answering the question the failed call was asking, with the file already resolved and read.",
   "fix": "The refusal lists the file's symbols and shows a concrete call. Ordered by name, never by the objective: an error message is not where retrieval belongs, and a test asserts two calls return identical text."
  },
  {
   "id": "F-81",
   "priority": "P1",
   "component": "tools / diagnosability",
   "status": "FIXED",
   "evidence": "6 of granite4.1:3b's 24 copy_code calls returned ERROR_PATH_OUTSIDE_REPO for paths that were merely wrong: 'poetry/console/commands/env/info.py' in a repo whose code lives under src/, 'tests/test_concurrency.py' in a checkout that has no such file.",
   "impact": "The harness told the engine its path was outside the repository when the path was only mistaken. False, alarming, and it teaches nothing -- the next call cannot be made right from that message.",
   "cause": "copy_code was the only caller of _resolve(must_exist=True). The guard reports 'does not exist' as a ContainmentError and _resolve maps every ContainmentError to ERROR_PATH_OUTSIDE_REPO. Introduced with copy_code.",
   "fix": "copy_code resolves without must_exist and lets _read_text raise ERROR_FILE_NOT_FOUND with the directory's real contents, exactly as read_file always has. A real '..' is still a containment violation.",
   "note": "Same class as A3: a safety error standing in for an ordinary mistake."
  },
  {
   "id": "F-82",
   "priority": "P0",
   "component": "affordance / selection",
   "status": "FIXED_MEASUREMENT_PENDING",
   "evidence": "Candidate recall separated from selection, 50 matched runs each. REACHED_THE_SYMBOL (called read_symbol or copy_code naming the file AND the symbol): 4B 20 cases -> 95% correct; granite4.1:3b 2 cases -> 100% correct. OPENED_THE_FILE only: 4B 28 -> 64%; granite 44 -> 7%. NEVER_OPENED_IT: 4B 2 -> 50%; granite 4 -> 0%.",
   "impact": "When either engine NAMES the symbol it is right essentially every time. Granite's whole deficit is that it names it twice in fifty where the 4B names it twenty times. That is an affordance problem, not an intelligence one -- and it means retrieval cannot fix the remaining gap, which is where effort would otherwise have gone.",
   "cause": "search_code's note named a CATEGORY of next action ('lee con read_symbol o read_file(start, end)'). The engines that turn that into a concrete call score 95%; the ones that do not, 7%.",
   "fix": "The note shows the call, filled in from the top candidate, and every candidate carries its symbol name. The copy_code line appears only when the mission is waiting for a file that does not exist, the same deterministic predicate legal_tools uses. The ranking caveat stays: an affordance that also became a claim of certainty would be worse than the abstraction it replaced."
  },
  {
   "id": "F-83",
   "priority": "P2",
   "component": "write discipline",
   "status": "FIXED",
   "evidence": "Of the reference engine's first eleven runs after F-75, two wrote their answer SIX and NINE times, at 17 and 20 turns; the rest wrote once or twice.",
   "impact": "F-75 removed a real trap and the door it opened was walked through.",
   "cause": "Nothing distinguished self-correction from re-guessing.",
   "fix": "A note, never a refusal (F-63), when a third write happens with no read, search or run since the previous one. A failed lookup does not count as learning: an error is feedback about the call, not information about the code."
  },
  {
   "id": "F-84",
   "priority": "P1",
   "component": "tools",
   "status": "OPEN_PATCH_WRITTEN",
   "evidence": "granite4.1:3b on frozen HEAD: reaches a symbol in 14 of 50 runs (was 2) and copy_code succeeds where it was failing, and the answers are now median 4982 characters, p90 17828, max 34408. Correct answers median 442 characters; wrong answers median 1762, with 15 of 35 over 3000. REACHED_THE_SYMBOL converts at 93%; OPENED_THE_FILE at 3%.",
   "impact": "copy_code solved transport and created a precision problem. It copies whole classes and whole files where one function was wanted.",
   "cause": "copy_code inherited read_symbol's behaviour on a class and not its F-70 warning -- and it needs the warning MORE, because it deliberately does not return the body, so an over-copy leaves nothing the agent can see.",
   "fix": "PATCH WRITTEN, NOT APPLIED. Held until all three engines had run identical code: applying it mid-sweep would have made granite's numbers incomparable with the 4B's and qwen2.5-coder's. Two notes from facts the call already has -- the symbol resolved to a class, with its methods named; and the copied region is most of the file. Notes, not refusals: copying a whole class is sometimes right, and F-63 is the standing lesson about refusing things that are sometimes right."
  },
  {
   "id": "F-85",
   "priority": "P0",
   "component": "tools / my own fix",
   "status": "FIXED",
   "evidence": "granite4.1:3b, 12 of 50 runs: t1 copy_code with the right file and no selector -> the catalogue lists the symbols and shows a worked example using names[0]; t2 copy_code with THAT name -- 'ALWAYS_NO_SPACE' when it wanted 'visit', 'LazyProxy' when it wanted '__getattr__'; t3 finish DONE. Three turns, confident, wrong.",
   "impact": "A defect I introduced in F-80 while fixing a different one. The listing was in file order on purpose -- I did not want ranking inside an error message -- and then I showed an example using the first name, which turned the ordering I had been careful about into a recommendation.",
   "cause": "An unranked list presented as a menu is worse than no list.",
   "fix": "The listing says it is in FILE ORDER and not by relevance, names no candidate, and points at the tool that DOES rank with the call filled in: search_code(query=..., path=<that file>), which F-79 made possible. The principle survives -- retrieval still does not happen in an error message."
  },
  {
   "id": "F-86",
   "priority": "P1",
   "component": "efficiency",
   "status": "MEASURED",
   "evidence": "Frozen HEAD -> with the completion and over-copy notes. granite: wall ratio 2.6x -> 1.2x, per-success 83s -> 23s, turns mean 5.3 -> 4.2. 4B: ratio 11.0x -> 9.0x, per-success 94s -> 40s. Scores unchanged within noise (4B 72 -> 70, granite 28 -> 28, qwen2.5 10 -> 8).",
   "impact": "Capability preserved, latency materially better. Conversion when the symbol is reached rose to 100% for both 3B engines and 88% for the 4B.",
   "cause": "42 of 50 runs had a correct answer and never called finish.",
   "fix": "Accepted under the promotion rule: capability >= baseline AND at least one of latency/tokens/turns improved. Both did."
  },
  {
   "id": "F-87",
   "priority": "P0",
   "component": "RESULT",
   "status": "REPRODUCED",
   "evidence": "granite4.1:3b, four runs on identical code, same 50-case corpus: 44/60, 48/64, 48/56, 48/60. Pooled +56/-30, n=86, McNemar exact p=0.0067. Mean 47% bare against 60% through GAFITAS. Latency ratio 0.9-1.2x -- FASTER than the bare model in two of four runs, because copy_code moves the bytes and the engine never generates the payload. Median 2 turns. 8-11s per success against 1242s the same morning.",
   "impact": "A second engine, a different vendor and a smaller tier, is amplified rather than preserved. The model was not touched (I15).",
   "cause": "Nine harness defects, not the engine. Reach went 2/50 -> ~29/50 and conversion once the symbol is reached is 96-100%.",
   "fix": "n/a -- this is the result, recorded so a later regression has something to be measured against."
  },
  {
   "id": "F-88",
   "priority": "P1",
   "component": "experiment integrity",
   "status": "FIXED",
   "evidence": "matched_granite_r1's evidence directory held 100 sealed tickets for a 50-case run: 50 from 2026-09-03 and 50 from 2026-09-04. Caught only because selection_analysis reported n=100.",
   "impact": "Scores were never affected -- those come from freshly written jsonl -- but every analysis reading the ticket directory saw two code versions as one run. r1's selection breakdown was wrong until this was found.",
   "cause": "Reusing a label APPENDED evidence instead of refusing. DECLARED == MATERIALIZED == CONSUMED == EVALUATED broke quietly, which is the worst way for it to break.",
   "fix": "run_matched.py refuses a label that already holds sealed tickets; --overwrite deletes them on purpose. The 50 contaminated tickets are PRESERVED under CONTAMINATED_matched_granite_r1_20260903 with a README, not deleted: a contaminated cohort is the record of how this harness can produce an invalid analysis in silence."
  },
  {
   "id": "F-89",
   "priority": "P1",
   "component": "hypothesis, mine, falsified",
   "status": "FALSIFIED_AND_REVERSED",
   "evidence": "TinyAgent (arXiv 2409.00608) measured that basic similarity RAG DECREASED small-model function-calling accuracy (74.88% against 78.89% with no retrieval), and my own v4 run moved granite's reach 13->15, which is flat. I concluded opening_candidates was probably dead weight and set out to remove it. The ablation says the opposite: without it granite falls from 60% to 34% and reach from ~29/50 to 10/38.",
   "impact": "Acting on external evidence without measuring it in our own system would have cost the result of the day.",
   "cause": "The v4 reading was taken before the shortlist named copy_code. External evidence about a different system is a hypothesis here, never a finding.",
   "fix": "Kept. The ablation decided it, not the argument."
  },
  {
   "id": "F-90",
   "priority": "P1",
   "component": "my own instrument",
   "status": "GAP_RECORDED",
   "evidence": "MULTI_TURN_TOOL_PERSISTENCE, goal mode, 13 declared tools: qwen2.5-coder:3b scores 70% valid / 100% acceptable, no stall, no premature finish, recovery ok. granite4.1:3b scores 65% / 100% on the same probe. On the real matched task the ORDER REVERSES and the gap is enormous: granite 60%, qwen2.5-coder 32%.",
   "impact": "The probe passes an engine that then fails the work, and ranks it ABOVE the engine that succeeds. Anything decided on the probe alone would have been decided backwards.",
   "cause": "The probe states a goal EVERY TURN. The real mission states one objective once and requires the agent to decide the whole sequence, including when it is done. qwen2.5-coder's dominant failure is calling finish on turn 1 before reading anything -- self-direction, which neither probe mode measures. The first version's blind spot was multi-turn; this one's is open-endedness.",
   "fix": "NOT FIXED. Recorded, because an instrument whose blind spot is known is usable and one whose blind spot is assumed away is not. A third mode -- one objective, no per-turn goal -- would measure it, and would cost a design that does not confound engagement with task difficulty."
  },
  {
   "id": "F-91",
   "priority": "P2",
   "component": "qwen2.5-coder:3b",
   "status": "FRONTIER_CANDIDATE",
   "evidence": "0% -> 8 -> 12 -> 16 -> 24 -> 28 -> 36 -> 32 across the campaign, against a bare arm of 44-56%. v8: reach 17/50 converting 88%, 18 runs that open the file and convert 6%, 15 that never open it. Of those 15, 9 call finish as their FIRST action and ignore a refusal that names the exact copy_code call with its arguments filled in.",
   "impact": "The third engine remains DEGRADED after every harness defect found for the other two was fixed.",
   "cause": "Eliminated by measurement: protocol (J, chosen by payload limit), payload channel (repaired), tool surface (legal_tools), naming (the concrete call is given), stalling (now caught at 6 repeats), finish semantics (summary no longer required), prompt size (granite runs the identical prompt and succeeds). What is left is task engagement, and the persistence probe cannot see it -- see F-90.",
   "fix": "NONE YET. Stated as a frontier CANDIDATE and not a frontier: the honest claim is that no further harness hypothesis has been tested, not that none exists. Cost did improve: 28s per success against 277s at v4."
  },
  {
   "id": "F-92",
   "priority": "P1",
   "component": "localprog/repair.py",
   "status": "FIXED",
   "evidence": "28 of 50 runs in one cohort, and present in six engines across 2338 sealed runs. Reproduced against the module byte for byte: a read_file whose path came back as the file name with the rest of the object and the closing fence glued onto it, and a grep whose pattern swallowed its own context and ignore_case arguments.",
   "impact": "A correct tool call with a Python literal in ONE argument had the argument NEXT TO IT destroyed. The model had chosen the right tool, the right file and the right symbol; the harness handed the tool a corrupted path and reported the model's error.",
   "cause": "Two independent defects compounding. (1) json.loads rejects True, False, None and single quotes -- which is what a Python-trained model writes when asked for a JSON object -- so the call fell through to the _terminal_string tier. (2) That tier treated 'the blob does not end in a brace' as 'the generation was truncated', and so ran the last string value to the end of the text. Any fence, note or explanation after a COMPLETE object therefore became part of the last argument. A truncated generation stops; it does not close its braces and then keep writing.",
   "fix": "Added a python_literal tier (ast.literal_eval, which parses literals and refuses names, calls and imports) ahead of _terminal_string, and narrowed the truncation branch to blobs whose object never closes. The suite's own red-team test caught what I missed: literal_eval keeps the LAST of a duplicated key silently, which is the argument-smuggling shape the JSON path already refuses, so duplicate keys are now rejected before evaluation.",
   "risk": "A payload containing unbalanced braces could make _closes_its_object read 'closed' and refuse a repair it would previously have made. That direction is deliberate: refusing yields an ordinary recoverable error, guessing yields a silently corrupted argument.",
   "dependencies": "none"
  },
  {
   "id": "F-93",
   "priority": "P2",
   "component": "localprog/tools.py copy_code",
   "status": "FIXED",
   "evidence": "18 runs across four engines, ten of them in one cohort. Every retry was the identical call with the name argument deleted.",
   "impact": "One wasted turn per occurrence, and the retry discarded the semantic anchor and kept only the arithmetic.",
   "cause": "read_symbol PRINTS the span it resolved. The model then calls copy_code with the name it asked for AND the line numbers it was just shown, which is agreement, not contradiction -- and the harness refused it as though the two could not both be meant. An interface demanding that the model UNSAY something it had got right.",
   "fix": "When the name resolves and the range equals the symbol's own span there is exactly one objective answer, so the call is completed. When they disagree the intent really is open -- a class name with one method's lines, a symbol with the whole file's range -- and it stays a refusal, now quoting the span the name actually has so the next call can be right without another read.",
   "risk": "None measured. Disagreement, a half-specified range, and an unresolvable name are all still refused, and the copy is never silently redirected to a range the model did not ask for.",
   "dependencies": "none"
  },
  {
   "id": "F-94",
   "priority": "P1",
   "component": "localprog/tools.py finish",
   "status": "FIXED",
   "evidence": "21 runs across four engines reached a symbol, never copied it, and were refused at finish. Two of them spent their entire budget on it: read_symbol resolved Installer._write_lock_file on turn 3 and then six consecutive finish(DONE) calls were refused, STALLED at turn 20; another resolved Env.base on turn 4, six refusals, BUDGET_EXHAUSTED at turn 20.",
   "impact": "The whole run lost, with the correct symbol already located and validated against the file.",
   "cause": "The refusal named the HARNESS's turn-zero candidate -- in both cases a different file from the one the agent had just successfully opened. So the message argued with a decision the agent had already made correctly, instead of completing it. The harness knew for certain that this agent had resolved symbol S in file F and that the answer file did not exist; the call was fully determined and it proposed a different one.",
   "fix": "read_symbol records every symbol the agent itself resolves. The refusal names the most recent of those, and falls back to the turn-zero candidate only when the agent has reached nothing. The shortlist is not removed -- its ablation is worth 60% against 20%, reach 29/50 against 9/50, p=0.0094 -- it just stops speaking over the agent. Writing the answer yourself is still offered, so the help stays additive.",
   "risk": "The named call is the one the agent already validated, so the next turn cannot fail on a symbol that does not exist. A test runs the proposed call to prove the harness would accept it.",
   "dependencies": "F-82, and the opening_candidates ablation"
  },
  {
   "id": "F-95",
   "priority": "P0",
   "component": "localprog/engine.py working_output",
   "status": "FIXED",
   "evidence": "One engine spent 170 of its 242 turns on ERROR_NO_TOOL_CALL -- 70% -- and scored 28% through GAFITAS against 66% bare. Its output usage: mean 916, median 974, max 1024 tokens per call, against a reference engine on the identical prompt at mean 61, median 46. 1024 is CONSERVATIVE_OUTPUT, the harness's own fallback.",
   "impact": "The single largest harness ceiling found. An engine that reasons before answering was cut off mid-thought on nearly every turn, returned empty content, and was told 'respuesta vacia; llama a una herramienta' -- blamed for a ceiling we chose. It was reported as DEGRADED -38 and the degradation was ours.",
   "cause": "An engine that returns its reasoning in a channel of its own pays for that reasoning out of the same output budget as the call. CONSERVATIVE_OUTPUT is a guess for engines that declare no limit, and for a reasoning engine the guess is a gag.",
   "fix": "emits_reasoning_channel is probed and recorded -- observed at the wire from whether the message carries a non-empty reasoning field, never inferred from the engine name -- and it governs exactly one number: the budget the harness guesses when the engine declares none. A declared max_output_tokens still wins, and the context-quarter ceiling is still hard. Two engines in the registry measure TRUE and two from the same families measure FALSE, which is why the property has to be measured.",
   "risk": "Latency. The smoke test measured a 3.7x wall-clock ratio against the bare arm at the larger budget, with output settling at 1524 tokens per call, so the new ceiling is a ceiling and not a cost -- but the reasoning itself is real time. Recorded, not traded away: the turn economy went from 26% real tool calls to 93%, and 170 empty turns to zero.",
   "dependencies": "F-96"
  },
  {
   "id": "F-96",
   "priority": "P0",
   "component": "REPOQA/run_matched.py",
   "status": "FIXED",
   "evidence": "The bare arm was called with a hardcoded num_predict=2048. The GAFITAS arm was called with caps.working_output(), which is 1024 for every engine that declares no limit. Measured consequence: one engine produced 2172 characters of reasoning and ZERO characters of answer at the budget the GAFITAS arm was running.",
   "impact": "A defect in my own instrument, not in GAFITAS. The two arms of a comparison whose entire claim is that GAFITAS is the only difference were given different output budgets, with twice the room going to the bare arm. It costs nothing where the answer is short, which is why it survived, and it decides the result where the engine reasons before answering. The five-engine expansion cohort's +20 AMPLIFIED and -38 DEGRADED were both produced by it and neither number survives.",
   "cause": "The bare arm's provider was constructed separately from provider_for() and never updated when working_output() was introduced.",
   "fix": "Both arms take the budget from the same place. The five-engine expansion cohort is recorded as measured through an unequal instrument and is excluded from conclusions; it is preserved in full, not deleted.",
   "risk": "Previously sealed cohorts are not comparable to anything measured after this, because the bare arm's budget changed for every engine. That is the price of the correction and it is stated rather than absorbed.",
   "dependencies": "F-95"
  },
  {
   "id": "F-97",
   "priority": "P3",
   "component": "REPOQA corpus",
   "status": "FALSIFIED_HYPOTHESIS_RECORDED",
   "evidence": "read_symbol reported ERROR_NO_MATCH for a nested symbol the bare arm answered correctly, which looked like the symbol index failing to walk nested function definitions. It does walk them. Checked all fifty needles against the checkout: 49 resolve, and that one is genuinely absent from the file at this commit.",
   "impact": "None on the harness. It is one case of corpus drift between the dataset's commit and the checkout, and it inflates the bare arm -- a model that dumps a large superset of the file can clear the 0.5 similarity threshold against a needle the file no longer contains, while a precise answer cannot.",
   "cause": "Dataset and checkout at different commits for one needle.",
   "fix": "Recorded, not patched. The hypothesis that the symbol index could not see nested definitions is FALSIFIED and kept as falsified rather than reinterpreted.",
   "risk": "none",
   "dependencies": "none"
  },
  {
   "id": "F-98",
   "priority": "P1",
   "component": "localprog/provider.py",
   "status": "FIXED",
   "evidence": "13 of 50 runs of one engine ended TIMEOUT at 300s and every one scored zero. The budget it was generating against was 8192 tokens.",
   "impact": "A quarter of that engine's cohort never happened, and its accuracy and its seconds-per-success were both computed over a denominator that included them. Its clean-cohort classification is INCONCLUSIVE as a result.",
   "cause": "The output budget and the patience were set in different places by different reasoning. Nothing connected 'we granted 8192 tokens' to 'we will wait 300 seconds'.",
   "fix": "timeout_for(num_predict, requested) returns a FLOOR derived from the budget at a conservative 12 tok/s -- well under the 121-126 tok/s measured on this machine -- bounded above at 1800s so a hung server is still a bounded failure. A caller asking to wait longer still wins, because that is a policy and this is a floor.",
   "risk": "A genuinely hung generation now takes longer to declare dead. Bounded by the ceiling, and preferable to scoring an engine zero for work it was still doing.",
   "dependencies": "F-95, F-96"
  },
  {
   "id": "F-99",
   "priority": "P0",
   "component": "localprog/loop.py",
   "status": "FIXED",
   "evidence": "316 turns on one engine and 196 on another produced no tool call. The harness recorded that fact, the error code and its own feedback -- and never what the model actually said.",
   "impact": "The single most expensive instrumentation gap in the campaign. Two engines' dominant failure mode was undiagnosable for an entire cohort, and one of them turned out to have a cause that took four lines to reproduce once the text existed.",
   "cause": "The event carried the harness's side of the exchange and not the engine's.",
   "fix": "The emission is sealed, bounded at 4000 characters, with the truncation announced rather than silent. An empty emission is distinguishable from a missing one, because 'it said nothing' and 'it said something unparseable' are different failures that used to look identical.",
   "risk": "Evidence files grow. Bounded, and the bound is stated in the record.",
   "dependencies": "none"
  },
  {
   "id": "F-100",
   "priority": "P1",
   "component": "localprog/loop.py",
   "status": "FIXED",
   "evidence": "Usage was aggregated per ticket, so 'how many generations hit the cap' could only be answered for single-call runs. The reasoning channel was counted inside eval_count, so the cost of thinking could not be separated from the cost of answering. seen_at was null in 234 of 252 events.",
   "impact": "Three questions the clean cohort could not be asked, one of which (cap-hits) turned out to decide an engine's whole result.",
   "cause": "Instrumentation written for totals, then used for distributions.",
   "fix": "Per-call usage, per-event elapsed seconds, and the reasoning channel recorded separately -- in CHARACTERS, because the server reports no token split and deriving one from a ratio would be a guess wearing a number. The bare arm records equivalent usage too, so a tokens-per-success comparison between the arms exists for the first time.",
   "risk": "none measured",
   "dependencies": "none"
  },
  {
   "id": "F-101",
   "priority": "P1",
   "component": "localprog/loop.py, run_matched.py",
   "status": "DETECTED_NOT_ACTED_ON",
   "evidence": "The moment per-call usage landed: gemma3:4b's bare arm produced exactly 1024 of 1024 tokens on 6 of 6 smoke cases, and 9 of 10 on a second run. Its clean-cohort bare score of 7/50 is therefore not a measurement of what it knows.",
   "impact": "F-95 raised the output budget only for engines with an observed reasoning channel. This engine has none and was being truncated anyway, so that rule was half the rule. Any comparison against a truncated bare arm flatters GAFITAS.",
   "cause": "CONSERVATIVE_OUTPUT is 1024 and it is the harness's own guess for engines that declare no limit. It is too small for a task whose answer is a function reproduced verbatim.",
   "fix": "NOT YET. Truncation is now recorded on both arms so it can be counted rather than inferred, and a ladder measures the smallest budget at which cap-hits reach zero. Picking a bigger number before measuring would be the same mistake in the other direction, which is exactly how F-95 ended up covering half the engines it needed to.",
   "risk": "Until this is settled the next cohort cannot be called fair for any engine whose bare arm truncates.",
   "dependencies": "F-95, F-100"
  },
  {
   "id": "F-102",
   "priority": "P0",
   "component": "localprog/transcript.py",
   "status": "FIXED",
   "evidence": "Measured at the wire, three samples per arm, same conversation: gemma3:4b answers 0 of 3 with the result under role=tool (eval 1, 1, 1, content empty) and 3 of 3 with the identical result under role=user (eval 20, 20, 20). Seven other engines read the tool role correctly.",
   "impact": "196 dead turns, a 33% real-call rate, 100% STALLED and a 0% finish rate across 50 runs. That engine was never able to see a single result the harness handed it.",
   "cause": "The harness delivers every tool result as role=tool, and that message is the LAST one before the model speaks. Some chat templates do not implement the role in that position; the engine receives nothing.",
   "fix": "tool_role_supported is measured and recorded, and when it is False the result arrives as a user message carrying the same bytes and the tool's name. UNKNOWN keeps the tool role: moving a channel on a guess is a defect already paid for once.",
   "risk": "MEASURED AND MIXED. Ten cases, same needles: STALLED 10/10 became FINISHED 10/10, real-call rate 33% to 95%, one-token turns gone entirely -- and the SCORE went 3 to 2, with turns rising from 55 to 84. This is a ROBUSTNESS fix. Calling it a capability fix because the green metrics turned green is the error the last two corrections were about.",
   "dependencies": "F-99, F-100"
  },
  {
   "id": "F-103",
   "priority": "P2",
   "component": "measure_tool_role.py",
   "status": "FALSIFIED_HYPOTHESIS_RECORDED",
   "evidence": "The first version of the tool-role probe appended a user turn AFTER the tool result. All eight engines passed, including the one that fails 3 of 3 when the probe matches the harness's real shape.",
   "impact": "The investigation into 512 dead turns would have closed with a clean bill of health. It also sampled once, which the payload ladder had already shown to be a coin flip.",
   "cause": "The probe did not reproduce the shape the harness actually produces: a conversation ENDING on the tool result. The failure was masked by the one message the harness never sends.",
   "fix": "The probe ends on the result, samples three times, and uses each engine's own output budget -- a fixed 512 had made both reasoning engines INCONCLUSIVE, which was F-95 reappearing inside the probe built to be free of it.",
   "risk": "Recorded as a standing rule: a probe that does not reproduce the harness's own shape measures nothing.",
   "dependencies": "F-90"
  },
  {
   "id": "F-104",
   "priority": "P0",
   "component": "campaign methodology",
   "status": "PARTIALLY_FALSIFIED",
   "evidence": "granite4.1:3b, the same 20 needles, differing only in the bare arm's output budget. At 1024 (the campaign's setting): bare 6, GAFITAS 11, W=8 L=3, net +5, with cap-hits measured at 3 of 8 on a separate ladder. At 8192: bare 9, GAFITAS 12, W=6 L=3, net +3, cap-hits 0 of 20.",
   "impact": "The MECHANISM is real and measured: granite4.1's bare arm hit the output cap in 3 of 8 cases and now hits it in 0 of 20. The SIZE I attributed to it -- roughly 40% of the net advantage -- does not survive. Cohort 2 gave a variance estimate I did not have when I wrote this: granite4.2:3b, same 50 cases, ESSENTIALLY IDENTICAL conditions between cohorts (it was already at 8192 on both arms, so neither F-101 nor F-102 touched it), moved bare 39->41 and net +4->+2. That is the same magnitude as the change I had credited to removing truncation on a different engine at n=20. So the +5->+3 shift is not distinguishable from run-to-run noise.",
   "cause": "F-101. CONSERVATIVE_OUTPUT of 1024 cut off the bare arm, which answers in one long generation, while never binding the GAFITAS arm, which spends a median 46 tokens per call.",
   "fix": "The budget fix stands on its own evidence -- cap-hits measured before and after. What is withdrawn is the quantification. Prior deltas remain upper bounds because the truncation was real, but 'about 40%' was me converting one 20-case difference into a number before I knew what a 50-case rerun does on its own.",
   "risk": "Recorded as a lesson: a before/after difference is not an effect size until the null rerun is measured. Cohort 2 supplies that null for one engine and it is +-2 on the net at n=50.",
   "dependencies": "F-96, F-100, F-101"
  }
 ],
 "safety_record": {
  "sealed_runs_analysed": 133,
  "scope_violations": 0,
  "harness_invalid": 0,
  "source_repos_modified": 0
 },
 "open": [
  "F-55 the DOGFOOD-03 class is above the local tier and is recorded as UNSUPPORTED",
  "the ga04 class (a 16-requirement module from a written spec) likewise",
  "Behavioral Oracle is still a separate authority, not wired in as extra conscience signals",
  "no commit/handoff step: the patch is produced, promotion stays a human decision",
  "Explorer never demonstrated as needed, so never integrated",
  "F-96 invalidates the five-engine expansion cohort's deltas; the cohort is preserved and excluded from conclusions",
  "latency cost of the F-95 reasoning budget is measured but not yet optimised",
  "F-101 unresolved: the bare arm truncates at 1024 for at least one engine, so no cohort is fair for it until the budget is settled by measurement",
  "qwen3:4b's 316 dead turns remain unexplained; the tool role is NOT the cause (7 of 8 engines read it correctly) and the sealed emission text now exists to diagnose them",
  "every pre-F-101 amplification figure is an upper bound; the bias is measured at roughly 40% of net advantage on one 20-case sample",
  "run-to-run variance on the paired net is about +-2 at n=50 (granite4.2, two cohorts, unchanged conditions); no single-cohort delta smaller than that supports a claim"
 ],
 "objective": "Remove the paid tier from the normal programming path. Luna and Claude become construction, teaching and audit tools; they are not runtime dependencies for the supported task classes.",
 "final_measurement": {
  "final_battery_new_tickets": "5/5 LOCAL, 0 paid calls",
  "benchmark_corpus_free_ladder": "7/8, 0 paid calls",
  "local_self_maintenance": "2/2 (DOGFOOD-04, DOGFOOD-05), both by qwen3:4b, 0 paid calls",
  "paid_share_for_supported_classes": 0.0
 }
}